services:
  - type: web
    name: buglens-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
